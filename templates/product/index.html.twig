{% extends 'base.html.twig' %}

{% block title %}Products | ProjetWebS4{% endblock %}

{% block body %}
    <h2>Products</h2>

    <div class="container">
        {% if products != null %}
            {% for product in products %}
            <div class="item"></div>
            {% endfor %}
        {% else %}
            <p>No products available...</p>
        {% endif %}
    </div>
{% endblock %}
